pkg_info() {
	_xDESC="ICU"

    _xVER_=60.2
    _xPKG_NAME_=icu4c

	_xFILE="icu4c-60_2-src.tgz"
	_xFILE_PATH=icu
	_xNAME=${_xPKG_NAME_}/${_xVER_}
}

pkg_build() {
	cd source && (
		configure --prefix=${PREFIX} --enable-strict --enable-shared && make -j 6 && make install
	)
}
